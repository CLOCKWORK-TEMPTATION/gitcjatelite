# GitChat AI - المحلل الذكي للمستودعات والتوثيق والفيديو

**GitChat AI** هو تطبيق ويب متطور وتفاعلي يتيح للمستخدمين تحليل مستودعات GitHub، حزم البرمجيات (NPM/PyPI)، روابط التوثيق، وفيديوهات يوتيوب باستخدام تقنيات الذكاء الاصطناعي (Google Gemini API). يهدف التطبيق إلى تبسيط فهم الكود المصدري والمحتوى التقني والإجابة على الأسئلة المعقدة بناءً على سياق حقيقي.

## 🚀 المميزات الرئيسية

1.  **تحليل المصادر المتعددة**:
    *   **GitHub Repos**: إدخال رابط أي مستودع عام (أو خاص عبر Token) لتحليل الأكواد.
    *   **Local Directory**: اختيار مجلد من جهازك المحلي لتحليله مباشرة دون رفعه.
    *   **Packages (جديد 📦)**: دعم حزم **NPM** و **PyPI** (مثل `react`, `pandas`)؛ فقط اكتب اسم الحزمة وسيجلب التطبيق المستودع الرسمي لها.
    *   **Documentation**: قراءة وتحليل صفحات التوثيق عبر الروابط (URL).

2.  **تحليل فيديو YouTube المتقدم (جديد 🔴)**:
    *   جلب النص المفرغ (Transcript) تلقائياً.
    *   **روابط زمنية تفاعلية (Timestamp Linking)**: عند ذكر توقيت في الإجابة (مثلاً [04:20])، يمكنك الضغط عليه للقفز فوراً لتلك اللحظة في الفيديو.
    *   **فصول ذكية (Smart Chapters)**: زر مخصص لتقسيم الفيديو الطويل إلى فصول مع عناوين وملخصات لكل فصل.

3.  **وضع المراجع الذكي (Code Reviewer Mode) (جديد 👨‍💻)**:
    *   تحويل شخصية الـ AI إلى "Senior Engineer" صارم.
    *   التركيز على اكتشاف الثغرات الأمنية، مشاكل الأداء، وانتهاكات Clean Code.
    *   **توليد ملفات التصحيح (Patches)**: إنشاء ملفات `.diff` أو `.patch` للحلول المقترحة، جاهزة للتحميل والتطبيق المباشر.

4.  **مستعرض ملفات تفاعلي**:
    *   عرض شجري للملفات والمجلدات مع بحث وفلترة حسب الامتداد.
    *   **Visual Diagram**: عرض مخطط بصري (Graph) لهيكلية المجلدات باستخدام Mermaid.js.

5.  **تجربة مستخدم غنية**:
    *   **تصدير المحادثة (جديد 📥)**: تحميل سجل الدردشة كاملاً كملف Markdown.
    *   تنسيق الأكواد (Syntax Highlighting).
    *   بحث فوري داخل سجل المحادثة.

## 🛠 التقنيات المستخدمة (Tech Stack)

*   **Frontend**: React (v19), TypeScript, Tailwind CSS.
*   **AI**: Google Gemini API (`gemini-3-flash`, `gemini-3-pro`).
*   **RAG**: تنفيذ مخصص لنظام Retrieval-Augmented Generation داخل المتصفح.
*   **APIs**: GitHub REST API, NPM Registry, PyPI JSON API.
*   **Integration**: YouTube IFrame API, File System Access API.
*   **Visualization**: Mermaid.js.

## 📁 هيكلية المشروع

```text
├── index.html          # نقطة الدخول
├── App.tsx             # المكون الرئيسي وإدارة الحالة
├── types.ts            # تعريف الأنواع
├── constants.ts        # الثوابت (System Prompts, Limits)
├── components/         # المكونات
│   ├── YouTubePlayer.tsx # مشغل الفيديو المدمج
│   ├── ChatBubble.tsx  # عرض الرسائل والأكواد والروابط الزمنية
│   ├── FileSidebar.tsx # متصفح الملفات
│   ├── SettingsModal.tsx # الإعدادات
│   └── MermaidDiagram.tsx # المخططات الهيكلية
└── services/           # الخدمات
    ├── packageService.ts # البحث عن الحزم (NPM/PyPI)
    ├── localFileService.ts # التعامل مع الملفات المحلية
    ├── ragService.ts     # منطق التقسيم والبحث الدلالي
    └── ...
```

## 📖 كيفية الاستخدام

1.  **مفتاح API**: تأكد من وجود `API_KEY` لـ Google Gemini في ملفات البيئة.
2.  **اختيار المصدر**: اختر (GitHub, Local, Package, URL, YouTube) من القائمة العلوية.
3.  **التدقيق**: فعّل خيار "المراجع الذكي" من الإعدادات إذا كنت تريد تدقيقاً صارماً للكود.
4.  **التفاعل**: اسأل عن الكود، اطلب مراجعة، أو استفسر عن محتوى الفيديو.
5.  **التصدير**: استخدم زر التصدير لحفظ المحادثة عند الانتهاء.

---
تم التطوير بواسطة فريق GitChat AI بمستوى احترافي.